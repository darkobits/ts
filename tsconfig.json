{
  "extends": "@darkobits/ts/tsconfig.json",
  "include": ["src"],
  "compilerOptions": {
    "baseUrl": "src",
    "outDir": "dist",
    // Type errors with `vite` and `vite-plugin-checker`.
    "skipLibCheck": true,
    "lib": [
      "DOM",
      "DOM.Iterable",
      "ESNext"
    ]
  }
}
